Full-radius pill button, ~56px tall. Primary = lime with near-black text (one per screen); secondary = dark outlined; ghost = text-only. Bottom-pinned for CTAs.

\`\`\`jsx
<Button variant="primary">Next</Button>
<Button variant="secondary" iconLeft="chevron-left" fullWidth={false}>Back</Button>
<Button variant="primary" disabled>Next</Button>
\`\`\`
