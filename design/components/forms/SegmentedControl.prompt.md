Pill segmented control. Selected segment inverts to white fill + black text (highest contrast = current choice); unselected is muted gray.

\`\`\`jsx
<SegmentedControl options={['Low','Medium','High']} value={effort} onChange={setEffort} />
\`\`\`
