Group-ride tracker map: accent route line, numbered checkpoints, and live rider markers (lead / sweep / members) positioned on a 380×340 viewBox. Ships with a demo scene; pass \`riders\`/\`checkpoints\` (each with x,y) to drive it.

\`\`\`jsx
<GroupMap height={400} />
\`\`\`